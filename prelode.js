// Safe preload script with no exposed Node APIs
window.addEventListener("DOMContentLoaded", () => {
  // This is where we could expose limited APIs to the renderer if needed
  // We're keeping this minimal for security
});
